import React from 'react';

export default function HeroTicker(): React.ReactElement { 
    return (
        <div className="hero-ticker bg-foreground text-background">
            <p>churrasco profissional em um só lugar.</p>
        </div>
    );
}
